# QuantForge research app — extra runtime dependencies
#
# These are NOT part of the framework wheel (apps/ is build-invisible);
# install them once with:
#   uv pip install --python .venv/bin/python -r apps/research/requirements.txt
#
duckdb>=1.5
pandas>=2.2
pyarrow>=14
tabulate>=0.9
yfinance>=1.6

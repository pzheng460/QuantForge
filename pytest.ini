[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
markers =
    critical: critical contract tests for cross-layer behavior
    slow: tests that fetch real market data (network required, slow)
